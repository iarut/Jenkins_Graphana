input {
    beats {
        port => 5044
        host => "0.0.0.0"
  }
}


output {
  elasticsearch {
    hosts => "elasticsearch:9200"
    user => elastic
    password => elastic
    index => "%{[@metadata][version]}-%{+YYYY.MM.dd}"
  }
}